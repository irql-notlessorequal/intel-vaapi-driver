Taken from the Mesa 3D Project, these XML files will generate
a set of structs and accessors, which makes submitting data to the GPU
much easier as we no longer have to rely on batching and bit manipulation
in order to set values directly.

Everything is done neatly behind the scenes.